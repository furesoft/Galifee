components
	-> registry change
	-> xmlfile change
	-> downloader
	-> shell executer

extracter
	-> zip/rar
	-> msi?

integrated update check with netsparkle?
	updatecheck component


check if application is installed:
	if is installed -> set installmode to uninstall
	if is not installed -> set installmode to install

load appbuilder from xml file
	<setup>
		<properties>
			
		</properties>
	</setup>
	
ui
	popup for closing
	move to mvvm